torch>=2.0.0
diffusers>=0.21.0
transformers>=4.30.0
accelerate>=0.20.0
safetensors>=0.3.0

# Note: Pillow (PIL) is required for image handling
# It's usually auto-installed with diffusers, but install explicitly if needed:
# pip install pillow

# Tested with:
# torch==2.4.1, diffusers==0.30.3, transformers==4.44.2
# accelerate==1.1.1, safetensors==0.4.5
